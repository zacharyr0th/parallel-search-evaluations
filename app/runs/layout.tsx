import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Evaluations · Parallel Search evaluations",
};
export default function RunsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
