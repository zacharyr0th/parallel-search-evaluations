import type { Metadata } from "next";
export const metadata: Metadata = { title: "Sign in · Parallel Search evaluations" };
export default function SignInLayout({ children }: { children: React.ReactNode }) {
  return children;
}
